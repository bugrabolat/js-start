function onClickButton(element,x){
    var screen = document.getElementById("screen");
    switch (x) {
        case 'a':
            screen.value += element.value;
            break;
    case 'b':
        screen.value += element.value;
      break;
      case 'c':
        screen.value += element.value;
      break;
      case 'd':
        screen.value += element.value;
      break;
      case 'e':
        screen.value += element.value;
      break;
      case 'f':
        screen.value += element.value;
      break;
    }
}